# Testing Plan

- Backend contract tests for auth, buses, routes, stops, locations, and ETA
- Shared happy-path cases reused across all backend implementations
- Validation tests for malformed payloads
- Authorization tests for protected endpoints
- Frontend integration tests against the FastAPI backend first
